<% layout("layouts/boilerplate.ejs")%>
    <body>
        <%- include("../includes/filters.ejs") %> 
        <!-- <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
               

                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                      </button>
                </div>
            </div>
        </nav> -->
        <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3 listings">
            <%for(let listing of allListings){%>
                <a href="/listings/<%=listing._id%>" class="card-linking">
                    <div class="card listing-card">
                        <img src="<%= listing.image.url%>" class="card-img-top" alt="listing-img">
                        <div class="card-img-overlay"></div>
                        <div class="card-body">
                            <p class="card-text">
                                <b>
                                    <%=listing.title%>
                                </b> <br>
                                &#x20B9;<%=listing.price.toLocaleString("en-IN")%>/night
                                    <i class="taxInfo"> +18% GST</i>
                            </p>
                        </div>
                    </div>
                </a>
                <%}%>
        </div>
    </body>